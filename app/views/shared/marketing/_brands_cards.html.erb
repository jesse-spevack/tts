<%# brands-cards-multi-column
    Renders a section with eyebrow, headline, subheadline, and a responsive multi-column grid
    of brand/investor cards.

    Locals:
      eyebrow:     (String, optional) small label above headline
      headline:    (String, optional) section heading
      subheadline: (String, optional) section description
      id:          (String, optional) section id attribute

    Usage: Provide brand cards in the yielded block.

    Example:
      render "shared/marketing/brands_cards", eyebrow: "Backed by the best", headline: "Funded by leading investors." do
        render "shared/marketing/brands_cards_item", text: "Focused capital.", footnote: "Investor in Acme." do
          <img src="/placeholder.webp" alt="Brand logo" class="h-8">
        end
      end
%>

<section class="py-16" <% if local_assigns[:id] %>id="<%= id %>"<% end %>>
  <div class="mx-auto w-full max-w-2xl px-6 md:max-w-3xl lg:max-w-7xl lg:px-10 flex flex-col gap-10 sm:gap-16">
    <% if local_assigns[:headline] %>
      <div class="flex max-w-2xl flex-col gap-6">
        <div class="flex flex-col gap-2">
          <% if local_assigns[:eyebrow] %>
            <div class="text-sm/7 font-semibold text-mist-700 dark:text-mist-400"><%= eyebrow %></div>
          <% end %>
          <h2 class="font-display text-[2rem]/10 tracking-tight text-pretty text-mist-950 sm:text-5xl/14 dark:text-white"><%= headline %></h2>
        </div>
        <% if local_assigns[:subheadline] %>
          <div class="text-base/7 text-mist-700 dark:text-mist-400 text-pretty"><%= subheadline %></div>
        <% end %>
      </div>
    <% end %>

    <div>
      <div class="grid grid-cols-1 gap-2 sm:grid-cols-2 sm:has-[>:last-child:nth-child(3)]:grid-cols-1 md:has-[>:last-child:nth-child(3)]:grid-cols-3 md:has-[>:nth-child(5)]:grid-cols-3 lg:has-[>:last-child:nth-child(4n)]:grid-cols-4 lg:has-[>:last-child:nth-child(4n-1):not(:nth-child(3n))]:grid-cols-4">
        <%= yield %>
      </div>
    </div>
  </div>
</section>
