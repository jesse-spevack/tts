<%# pricing-hero-multi-tier
    Renders a pricing hero section with a monthly/annual toggle (via <el-tab-group>),
    headline, subheadline, and tab panels containing plan cards.

    Uses @tailwindplus/elements Web Components:
      <el-tab-group>, <el-tab-list>, <el-tab-panels>

    Locals:
      eyebrow:     (String, optional) small label above headline
      headline:    (String) section heading (e.g. "Pricing")
      subheadline: (String) section description
      id:          (String, optional) section id attribute
      options:     (Array<String>) tab labels, e.g. ["Monthly", "Yearly"]

    Usage: Provide plan cards for each option in the yielded block.
    The block is called once -- you must provide ALL tab panels inside it,
    wrapped in the appropriate structure.

    Example:
      render "shared/marketing/pricing_hero_multi",
            headline: "Pricing",
            subheadline: "Choose the plan that works for you.",
            options: ["Monthly", "Yearly"] do
        Monthly plans panel
        <div class="grid grid-cols-1 gap-2 ...">
          render "shared/marketing/pricing_hero_multi_plan", ...
        </div>
        Yearly plans panel
        <div class="grid grid-cols-1 gap-2 ...">
          render "shared/marketing/pricing_hero_multi_plan", ...
        </div>
      end
%>

<section class="py-16" <% if local_assigns[:id] %>id="<%= id %>"<% end %>>
  <el-tab-group>
    <div class="mx-auto w-full max-w-2xl px-6 md:max-w-3xl lg:max-w-7xl lg:px-10 flex flex-col gap-16">
      <div class="flex flex-col items-center gap-6">
        <% if local_assigns[:eyebrow] %>
          <div class="text-sm/7 font-semibold text-mist-700 dark:text-mist-400"><%= eyebrow %></div>
        <% end %>
        <h1 class="font-display text-5xl/12 tracking-tight text-balance sm:text-[5rem]/20 text-mist-950 dark:text-white"><%= headline %></h1>
        <div class="text-lg/8 text-mist-700 dark:text-mist-400 flex max-w-xl flex-col gap-4 text-center">
          <%= subheadline %>
        </div>
        <el-tab-list class="flex items-center gap-1 rounded-full bg-mist-950/5 p-1 dark:bg-white/5">
          <% options.each do |option| %>
            <button type="button" class="rounded-full px-4 py-1 text-sm/7 font-medium text-mist-950 aria-selected:bg-mist-950 aria-selected:text-white dark:text-white dark:aria-selected:bg-white/10 dark:aria-selected:text-white">
              <%= option %>
            </button>
          <% end %>
        </el-tab-list>
      </div>
      <el-tab-panels>
        <%= yield %>
      </el-tab-panels>
    </div>
  </el-tab-group>
</section>
