<%# Feature item for use inside _features_large_demo.html.erb
    Each item has an optional icon, headline, and subheadline text.

    Locals:
      headline:    (String) feature title
      subheadline: (String) feature description

    Usage: Provide the icon content in the yielded block (optional).

    Example:
      render "shared/marketing/features_large_demo_item", headline: "Fast", subheadline: "Lightning speed." do
        <svg ...>...</svg>
      end
%>

<div class="flex flex-col gap-2 text-sm/7">
  <div class="flex items-center gap-3 text-mist-950 dark:text-white">
    <% if block_given? %>
      <div class="size-3.25 h-lh"><%= yield %></div>
    <% end %>
    <h3 class="font-semibold"><%= headline %></h3>
  </div>
  <div class="flex flex-col gap-4 text-mist-700 dark:text-mist-400"><%= subheadline %></div>
</div>
