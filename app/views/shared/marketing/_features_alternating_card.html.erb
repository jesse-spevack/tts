<%# Feature card for use inside _features_alternating.html.erb
    Alternating layout card with demo image and text content.
    Uses CSS group-even to alternate the demo position on desktop.

    Locals:
      headline:    (String) feature title
      subheadline: (String) feature description
      cta_text:    (String, optional) link text
      cta_href:    (String, optional) link URL

    Usage: Provide the demo/screenshot content in the yielded block.

    Example:
      render "shared/marketing/features_alternating_card", headline: "Smart Replies", subheadline: "AI-powered responses." do
        <img src="/placeholder.webp" alt="" class="w-full">
      end
%>

<div class="group grid grid-flow-dense grid-cols-1 gap-2 rounded-lg bg-mist-950/2.5 p-2 lg:grid-cols-2 dark:bg-white/5">
  <div class="flex flex-col justify-between gap-6 p-6 sm:gap-10 sm:p-10 lg:p-6 lg:group-even:col-start-2">
    <div class="text-xl/8 sm:text-2xl/9">
      <h3 class="text-mist-950 dark:text-white"><%= headline %></h3>
      <div class="flex flex-col gap-4 text-mist-500"><%= subheadline %></div>
    </div>
    <% if local_assigns[:cta_text] && local_assigns[:cta_href] %>
      <a href="<%= cta_href %>" class="inline-flex items-center gap-2 text-sm/7 font-medium text-mist-950 dark:text-white">
        <%= cta_text %>
        <svg width="6" height="10" viewBox="0 0 6 10" fill="none" stroke="currentColor" class="inline-block" stroke-width="1"><path d="M1 1L5 5L1 9" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </a>
    <% end %>
  </div>
  <div class="relative overflow-hidden rounded-sm lg:group-even:col-start-1 dark:after:absolute dark:after:inset-0 dark:after:rounded-sm dark:after:outline-1 dark:after:-outline-offset-1 dark:after:outline-white/10">
    <%= yield %>
  </div>
</div>
