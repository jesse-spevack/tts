<div class="max-w-xl mx-auto px-4 py-16 text-center">
  <div class="mx-auto flex size-16 items-center justify-center rounded-full bg-mist-950/10 dark:bg-white/10 mb-6">
    <%= render "shared/icons/heart", css_class: "size-8 text-mist-950 dark:text-white" %>
  </div>

  <% if Current.user.has_credits? && !Current.user.premium? %>
    <h1 class="font-display text-2xl tracking-tight text-mist-950 dark:text-white mb-4">
      Credits added!
    </h1>

    <p class="text-lg text-mist-500 dark:text-mist-400 mb-8">
      You now have <strong><%= Current.user.credits_remaining %> episode credits</strong>. Up to 50,000 characters each.
    </p>
  <% else %>
    <h1 class="font-display text-2xl tracking-tight text-mist-950 dark:text-white mb-4">
      Welcome to Premium
    </h1>

    <p class="text-lg text-mist-500 dark:text-mist-400 mb-8">
      Thank you! Unlimited episodes, up to 50,000 characters each.
    </p>
  <% end %>

  <%= link_to "Start creating episodes", new_episode_path,
    class: button_classes(type: :primary) %>
</div>
