<%# locals: (n:) %>
<%# Numbered circle that anchors each step heading on the splitting-articles
    help page. Pulled out to a partial only because it appears 4 times. %>
<div aria-hidden="true"
     class="flex size-8 items-center justify-center rounded-full bg-mist-950 text-sm font-semibold text-white dark:bg-white dark:text-mist-950 tabular-nums">
  <%= n %>
</div>
