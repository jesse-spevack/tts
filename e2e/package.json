{
  "name": "tts-e2e-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "posttest": "echo '\n💡 To clean up test users: bin/rake test:purge_e2e_users'",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.3.0"
  }
}
