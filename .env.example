# Google Cloud Configuration
# Required for both TTS and Cloud Storage functionality

# Your Google Cloud Project ID
# Find this in: Google Cloud Console > Home > Project Info
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Cloud Storage bucket name for podcast episodes and feed
# Create bucket: https://console.cloud.google.com/storage/browser
# Bucket naming: lowercase letters, numbers, hyphens only (no underscores)
GOOGLE_CLOUD_BUCKET=your-bucket-name

# Path to your Google Cloud service account credentials JSON file
# Create credentials:
#   1. Go to: https://console.cloud.google.com/iam-admin/serviceaccounts
#   2. Click "Create Service Account"
#   3. Name it (e.g., "podcast-tts-service")
#   4. Grant roles:
#      - "Storage Object Admin" (for reading/writing files)
#      - "Cloud Text-to-Speech User" (for TTS API)
#   5. Click "Done"
#   6. Click on the service account email
#   7. Go to "Keys" tab
#   8. Click "Add Key" > "Create new key" > "JSON"
#   9. Save the downloaded JSON file to your project directory
#   10. Set the path below (relative or absolute)
GOOGLE_APPLICATION_CREDENTIALS=./path/to/your-credentials.json

# Important Security Notes:
# - Never commit your actual .env file or credentials JSON to version control
# - Add .env and *-credentials.json to your .gitignore
# - Keep your service account key secure and rotate it periodically

# API Configuration
# Generate with: openssl rand -hex 32
API_SECRET_TOKEN=your-secret-token-here

# Cloud Tasks Configuration
CLOUD_TASKS_LOCATION=us-west3
CLOUD_TASKS_QUEUE=episode-processing

# Service URL (set automatically during deployment)
SERVICE_URL=
